import MazeMap from "../components/maze/mazeMap"
import Directions from "../types/directions"
import { IndexObject } from "../types/indexObject"
import PossibleTiles from "../types/possibleTiles"

const getAdjacentTiles = (positionXY: [number, number]) => {
  const x = positionXY[0];
  const y = positionXY[1];

  // const adjacentTiles: IndexObject<Directions, PossibleTiles> = {
  //   up: MazeMap.mazeMapXY
  // }
}


export default {}